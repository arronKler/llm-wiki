interface:
  display_name: "Wiki Ingest"
  short_description: "Capture sources into the managed local wiki"
  default_prompt: "Use $wiki-ingest to capture this source and integrate it into my managed local wiki."
